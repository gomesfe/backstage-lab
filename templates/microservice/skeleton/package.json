{
  "name": "${{ values.nome }}",
  "version": "0.1.0",
  "private": true,
  "description": "${{ values.descricao }}",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  }
}
